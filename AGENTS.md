## Dev environment tips
- We use uv for package management in this project, so use uv run to run code
- Data files and model files are large, for this reasons they are downloaded with entry points

## Coding style
- Use type-hints when possible
- Avoid relative and circular imports when possible